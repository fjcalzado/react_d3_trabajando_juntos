import * as React from 'react';

const style = require("./countries.style.scss");

export const CountriesComponent = () => {
  return (
    <h2 className={style.container}>Implementation Pending</h2>
  );
}
